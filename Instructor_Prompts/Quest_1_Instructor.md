# Quest 1: Instructor Test Prompt

*This is a pre-generated, highly detailed prompt simulating what ChatGPT would produce. The instructor can feed this directly to Antigravity for live demos without needing ChatGPT.*

> Act as a senior DevOps engineer. Please execute the following sequence of commands in the terminal:
> 1. Run `git init` to initialize a repository.
> 2. Create a `README.md` file with the content: "# CyberRaid Workshop\nWelcome to the next generation of web development."
> 3. Run `git add .` and `git commit -m "Initial commit"`
> 4. Run `git remote add origin https://github.com/SanketSalviNITK/cyberraid.git`
> 5. Run `git push -u origin main`
