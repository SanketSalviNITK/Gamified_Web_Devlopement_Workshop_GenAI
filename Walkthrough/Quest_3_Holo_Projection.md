# Quest 3: Holo Projection (3D Web)

**Objective**: Integrate the Three.js library into your portfolio to render a basic, animated 3D scene in the background.

---

## Step 1: The Meta-Prompt (Use in ChatGPT)
*Copy this prompt, replace the bracketed placeholders with your choices, and paste it into **ChatGPT**.*

> Act as an expert Prompt Engineer. I need to instruct my local AI coding assistant (Antigravity) to add 3D graphics to my website. Please generate a highly detailed prompt formatted as a Markdown file. 
> 
> The task is to: Modify my existing `index.html` file to include a Three.js canvas as a fixed background behind my glassmorphism UI.
> 
> Make sure the instructions tell the AI to:
> - Inject the Three.js CDN script tag into the HTML.
> - Write a new javascript file called `bg.js` that initializes a Three.js scene, camera, and renderer.
> - The scene should contain a slowly rotating **[3D_SHAPE_e.g._TORUS_KNOT_OR_CUBE]** that has a glowing **[COLOR]** material.
> - Ensure the canvas is styled to be `position: fixed`, `z-index: -1`, covering the whole screen.
> 
> Output ONLY the raw markdown content so I can save it directly into a file.

## Step 2: Execute with Antigravity
*Take the markdown generated by ChatGPT and save it in your project folder as `quest_3_instructions.md`. Then, use this simple prompt with **Antigravity**:*

> Read the file `quest_3_instructions.md` and execute the steps to add the 3D background to my project.
